import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.attendanceactivity.app',
  appName: 'activity_attendance',
  webDir: 'out'
};

export default config;
